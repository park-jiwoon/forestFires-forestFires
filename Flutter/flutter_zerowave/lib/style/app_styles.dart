import 'package:flutter/material.dart';


// AppBar 타이틀 스타일
final TextStyle appBarTitleTextStyle = TextStyle(
  color: Colors.white,
  fontWeight: FontWeight.bold,
  fontSize: 20,
);

// AppBar 메뉴 아이콘 색상
final Color appBarIconColor = Colors.white;

// AppBar 텍스트 버튼 스타일
final TextStyle appBarButtonTextStyle = TextStyle(
  color: Colors.white,
  fontWeight: FontWeight.bold,
);

// AppBar 텍스트 버튼 간격
final EdgeInsets appBarButtonPadding = EdgeInsets.symmetric(horizontal: 0);

// 배너 배경색
final Color bannerBackgroundColor = Color(0xFF009966);

// 배너 텍스트 스타일
final TextStyle bannerTextStyle = TextStyle(
  fontFamily: 'InkLipquid',
  fontSize: 20, // 1.7em in Flutter, assuming font size base is 16px
  color: Colors.white,
);


// 로고 배경색
final Color logoBackgroundColor = Colors.white;

// 로고 컨테이너 패딩
final EdgeInsets logoPadding = EdgeInsets.symmetric(horizontal: 10, vertical: 0);

// 로고 컨테이너 너비
final double logoWidth = 50;
final double logoHeight = 80;

// 텍스트 패딩
final EdgeInsets textPadding = EdgeInsets.symmetric(horizontal: 20);

// 하단 버튼 배경색
final Color bottomButtonBackgroundColor = Color(0xFF44e389);

// 하단 버튼 텍스트 스타일
final TextStyle bottomButtonTextStyle = TextStyle(
  color: Colors.black,
  fontSize: 20.0,
  fontWeight: FontWeight.bold,
);

// 하단 버튼의 높이
final double bottomButtonHeight = 48.0;

// 하단 버튼의 둥근 모서리 반경
final double bottomButtonBorderRadius = 8.0;

// 하단 테두리 색상
final Color borderColor = Color(0xFFDCDCDC);


// 첨부할 사진 또는 동영상을 선택해주세요
final TextStyle cameraScreenText = TextStyle(
  color: Colors.black,
  fontSize: 18.0,
  fontWeight: FontWeight.bold,
);

final TextStyle CameraModalContentText = TextStyle(
  color: Colors.black,
  fontSize: 16.0,
);